# Released Native WhisperX Boundary

Subtitle Merger consumes native-whisperx as a pinned published Cargo release,
not a developer checkout. Multi-language translation is enabled only when the
required released library version supports its model registry and cancellation
contract. This avoids making a local checkout an undeclared runtime dependency.
